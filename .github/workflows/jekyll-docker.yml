- name: Build with Emscripten
  run: |
    docker run --rm -v $(pwd):/src emscripten/emsdk emcc main.cpp -o cnc.js \
    -s EXPORTED_FUNCTIONS='["_calculate"]' \
    -s EXPORTED_RUNTIME_METHODS='["ccall","cwrap"]' \
    -s SINGLE_FILE=1
